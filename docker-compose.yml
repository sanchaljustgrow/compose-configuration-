version: "3.8"

services:
  angular-frontend:
    image: sanchaljustgrow/docker-testnew-frontend:latest   # 👈 Pulls from Docker Hub
    container_name: angular-frontend
    ports:
      - "8080:80"
    environment:
      - API_URL=https://dummyjson.com/products/2
